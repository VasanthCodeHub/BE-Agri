"""Direct contact between a user and a provider.

The product has NO bookings: a user finds a vehicle, taps call, and the two
sides negotiate directly. This module records that contact (so the provider
dashboard can count it) and surfaces the provider's number to the caller.
"""
