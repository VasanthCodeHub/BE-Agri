"""Vehicle master data — manufacturers, models, variants.

The data the app's "add a vehicle" form is built from: the frontend renders
dropdowns from `GET /vehicle-masters` instead of hard-coding brand names.
"""
